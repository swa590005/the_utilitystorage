{% extends 'content_base.html.twig' %}

{% block content_body %}
    <h1>Create a new MeterData! </h1>
    {{ form_start(meterForm) }}
    {{ form_row(meterForm.meter_number) }}
        <button type="submit" class="btn btn-primary">Create!</button>
    {{ form_end(meterForm) }}
{% endblock %}
